DATABASE_URL=file:.data/data.db
# Auth (dev only — works with npm run dev, not in static export)
# AUTH_SECRET=  (run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# AUTH_GITHUB_ID=你的 GitHub OAuth App Client ID
# AUTH_GITHUB_SECRET=你的 GitHub OAuth App Client Secret
# Phase 2:
# AI_API_KEY=sk-...
# AI_MODEL=claude-sonnet-4-6
